# Directory listing
alias ll='ls -halF'
alias la='ls -A'
alias l='ls -CF'

# Test network
alias tnet='wget --spider http://www.google.com'

# Apt updates
alias aptupdate='sudo apt update && apt list --upgradable'
